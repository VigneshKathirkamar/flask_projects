apiVersion: v1
kind: Service
metadata:
  name: flask-service
spec:
  type: NodePort
  selector:
    app: flask-app
  ports:
  - protocol: "TCP"
    port: 6000
    targetPort: 5000 #The target port has to match the container port on deployment.yaml